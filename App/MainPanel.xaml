<UserControl x:Class="App.MainPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Width="Auto"
             Height="Auto" 
             d:DesignWidth="300" 
             d:DesignHeight="30">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <WrapPanel Grid.Column="0" Grid.Row="0" x:Name="scriptsPanel" Margin="0,0,0,0">
            <Button x:Name="addScriptBtn" Content="Add Script" Margin="0,0,0,0" Width="100" Height="20" Click="onAddScript" />
        </WrapPanel>
        <Button Grid.Column="1" Grid.Row="0" Content="Config" Margin="0,0,0,0" VerticalAlignment="Top" Width="50" Height="20"  Click="Button_Click"/>
    </Grid>

</UserControl>

